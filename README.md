# VkinderBot
## 🎯 Что это?
VkinderBot — это бот для ВКонтакте, который помогает пользователям находить потенциальных кандидатов для знакомства на основе указанных предпочтений (пол, возраст, город). Бот показывает анкеты с фотографиями, позволяет добавлять пользователей в избранное или чёрный список и просматривать сохранённых кандидатов.

## ⚡ Быстрый старт
```bash
# 1. Установите зависимости
pip install -r requirements.txt

# 2. Настройте переменные окружения
echo "VK_TOKEN=ваш_токен" >> .env
echo "BOT_TOKEN=ваш_токен_бота" >> .env
echo "POSTGRES_HOST=localhost" >> .env
echo "POSTGRES_PORT=5432" >> .env
echo "POSTGRES_USER=ваш_пользователь" >> .env
echo "POSTGRES_PASSWORD=ваш_пароль" >> .env
echo "POSTGRES_URI=postgresql://ваш_пользователь:ваш_пароль@localhost:5432/vk_dating_bot_db" >> .env

# 3. Создайте базу данных
python -m core.init_db_for_vk_dating_bot

# 4. Создайте таблицы
python -m core.models

# 5. Запустите бота
python basic_code.py
```

## 📦 Установка

### Требования
- Python 3.9+
- PostgreSQL 12+

### Установка зависимостей
```bash
pip install -r requirements.txt
```

### Конфигурация базы данных
Убедитесь, что PostgreSQL сервер запущен и доступен. Бот использует SQLAlchemy для работы с БД.

## 🚀 Использование

Запустите бота командой:
```bash
python basic_code.py
```

После запуска отправьте боту в ВКонтакте сообщение `/start`.

### Основной сценарий
1. Бот предложит выбрать пол кандидата.
2. Укажите свой возраст.
3. Нажмите кнопку "Дальше" для просмотра анкет.
4. Используйте кнопки:
   - ❤️ В избранное — сохранить кандидата
   - ⛔️ В ЧС — добавить в чёрный список
   - ⭐️ Избранное — посмотреть сохранённых
   - 🔄 Сменить настройки — начать настройку заново

## 🔧 Конфигурация

Конфигурация задаётся через переменные окружения в файле `.env`:

| Переменная | Описание |
|------------|----------|
| `VK_TOKEN` | Токен пользователя для доступа к API ВКонтакте (для поиска) |
| `BOT_TOKEN` | Токен сообщества для работы с Long Poll API |
| `POSTGRES_URI` | Строка подключения к PostgreSQL |
| `POSTGRES_HOST` | Хост базы данных |
| `POSTGRES_PORT` | Порт базы данных |
| `POSTGRES_USER` | Пользователь базы данных |
| `POSTGRES_PASSWORD` | Пароль пользователя базы данных |

## 📁 Структура проекта
```
.
├── basic_code.py          # Основной код бота
├── config.py              # Настройки приложения
├── core/                  # Ядро приложения
│   ├── base_repository.py # Базовые CRUD-операции
│   ├── candidate_crud.py  # Логика работы с кандидатами
│   ├── init_db_for_vk_dating_bot.py # Создание БД
│   ├── models.py          # Модели SQLAlchemy
│   └── user_crud.py       # Логика работы с пользователями
├── docs/                  # Документация
├── requirements.txt       # Зависимости
├── vk_dating_bot_db.sql   # SQL-дамп базы данных
└── vkapi.py               # Клиент для API ВКонтакте
```
